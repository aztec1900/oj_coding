#define _CRT_SECURE_NO_WARNINGS
#include <iostream>
#include <cstdio>

#define MAXN 1000005

using namespace std;

typedef long long ll;

ll dp[MAXN];

int main() {
	int m, n;
	while (scanf("%d %d", &m, &n) != EOF) {
		memset(dp, 0, sizeof(ll)*MAXN);
		for (int i = 0; i < n; ++i) {

		}
	}
}